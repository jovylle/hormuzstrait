# Local environment files (never commit secrets)
.env
.env.local
.env.development
.env.production

# Node / tooling artifacts (future-proofing)
node_modules
dist
.DS_Store

# Local Netlify folder
.netlify

# Hormuz Traffic Monitor – local env template

# OilPrice API — only for npm run update-oil + GitHub Actions (writes data/oil-history.json).
# Not used on every site visit. Local: copy to .env then npm run update-oil
OILPRICE_API_KEY=

# Netlify function environment (optional, if you want to mirror)
# HORMUZ_DASHBOARD_URL=https://hormuzstraitmonitor.com/api/dashboard